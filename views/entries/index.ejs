<% include ../head %>

<ul>
  <li>
    <a href='/entries/new' >Create New Entry</a>
  </li>

<% for(var i=0; i<entries.length; i++) { %>
 <li>
   <a href='/entries/<%= entries[i].id %>' ><%= entries[i].slug %></a> &mdash;
   <a href='/entries/<%= entries[i].id %>/edit'>Edit</a> &mdash;
   <a href='/entries/<%= entries[i].id %>/delete' onclick='return confirm("Are you sure?")'>Delete</a>
 </li>
<% } %>
</ul>
<% include ../footer %>
